import { api } from '../../Services/api';
import {
    RESPONSE_TYPE,
    REDIRECT_URI,
    CLIENT_ID,
    CDN_IMAGE,
    SCOPE,
} from './DiscordAuth';

export {
    RESPONSE_TYPE,
    REDIRECT_URI,
    CLIENT_ID,
    CDN_IMAGE,
    SCOPE,
}